export const environment = {
  production: true,
  googleMapsApiKey: '', // Empty for demo mode - no API key required
  apiBaseUrl: '' // For GitHub Pages, leave empty to use the built-in demo storage fallback
};

